import 'package:flutter/material.dart';

class MyEventsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("My Events")),
      body: Center(child: Text("Welcome to the My Events Page!")),
    );
  }
}
